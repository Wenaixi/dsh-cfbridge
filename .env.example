# 复制为 DSH_HOME/.env（默认：%USERPROFILE%\.dsh\.env），不要放在项目目录或提交到 Git。
# 推荐最小权限。account token 至少包含 Account Resources: Read；DNS 操作使用 Zone:DNS 用户 token。
CLOUDFLARE_API_TOKEN=replace-with-your-cloudflare-api-token
